name: Echo Job

on: [push]

jobs:
  echo-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Echo Message
        run: echo "Hello, GitHub Actions!"
